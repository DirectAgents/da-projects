@model WorkflowModel

<h2>Campaign Summary</h2>

Accounting Period: <b>@Model.CurrentEomDateString</b><br />
@if (Model.AccountManagerId.HasValue)
{
    <text>Account Manager: </text><b>@Model.AccountManagerName</b><br />
}
<br />

Click on an advertiser to generate an invoice request.<br />
Hover over a campaign to see its display name (as it will appear in an invoice).<br />
<br />

@{
    ViewBag.RequestingInvoice = true;
}
@Html.Partial("../Workflow/CampaignAmounts", Model.CampaignAmounts)

<br />
Go to:
@Html.ActionLink("Workflow View", "Index", "Workflow", new { am = Model.AccountManagerId }, new { target = "_blank" })