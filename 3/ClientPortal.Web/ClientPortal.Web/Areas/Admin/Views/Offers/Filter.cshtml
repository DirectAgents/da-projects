@model IEnumerable<ClientPortal.Data.Contexts.CakeContact>
@{
    var ajaxOptions = RazorHelpers.GetAjaxOptions("cpmOffersTab");
}

Filter by Account Manager:<br />
@foreach (var am in Model)
{
    @Ajax.ActionLink(am.FullName, "Index", "Offers", new { am = am.CakeContactId }, ajaxOptions)<br />
}

<br />
Show:
<br />
@Ajax.ActionLink("all cpm offers", "Index", "Offers", ajaxOptions)
(
@Ajax.ActionLink("just those with campaigns", "Index", "Offers", new { mincampaigns = 1 }, ajaxOptions)
)

<br />
@Ajax.ActionLink("all offers", "Index", "Offers", new { cpmonly = false }, ajaxOptions)
(
@Ajax.ActionLink("just those with campaigns", "Index", "Offers", new { mincampaigns = 1, cpmonly = false }, ajaxOptions)
)
